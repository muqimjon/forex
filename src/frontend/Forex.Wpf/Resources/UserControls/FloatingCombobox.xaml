<UserControl x:Class="Forex.Wpf.Resources.UserControls.FloatingComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Height="30"
             d:DesignHeight="32"
             d:DesignWidth="100">

    <Grid>
        <!-- Floating label -->
        <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Style="{StaticResource FloatingLabelStyle}"
                   Tag="{Binding ElementName=comboBox}"
                   IsHitTestVisible="False"
                   Panel.ZIndex="1" />

        <!-- ComboBox -->
        <ComboBox x:Name="comboBox"
                  Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  IsEditable="True"
                  Padding="4"
                  Height="30"
                  BorderThickness="1"
                  BorderBrush="Gray" />
    </Grid>
</UserControl>
