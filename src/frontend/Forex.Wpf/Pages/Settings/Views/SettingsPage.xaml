<Page x:Name="RootPage"
      x:Class="Forex.Wpf.Pages.Settings.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
      xmlns:local="clr-namespace:Forex.Wpf.Pages.Settings"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SettingsPage">

    <Page.Resources>
        <Style TargetType="CheckBox"
               x:Key="PersistentCheckBoxStyle">
            <Setter Property="Foreground"
                    Value="Black" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderBrush"
                    Value="Gray" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="2"
                                    Width="16"
                                    Height="16"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center" />
                            <Path x:Name="CheckMark"
                                  Data="M0,4 L2,6 L6,0"
                                  Stroke="Black"
                                  StrokeThickness="2"
                                  Visibility="Collapsed"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked"
                                     Value="True">
                                <Setter TargetName="CheckMark"
                                        Property="Visibility"
                                        Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="RadioDotStyle"
               TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid>
                            <Ellipse x:Name="Outer"
                                     Width="16"
                                     Height="16"
                                     Stroke="Gray"
                                     StrokeThickness="1"
                                     Fill="Transparent"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center" />
                            <Ellipse x:Name="Inner"
                                     Width="8"
                                     Height="8"
                                     Fill="Black"
                                     Visibility="Collapsed"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked"
                                     Value="True">
                                <Setter TargetName="Inner"
                                        Property="Visibility"
                                        Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled"
                                     Value="False">
                                <Setter TargetName="Outer"
                                        Property="Stroke"
                                        Value="LightGray" />
                                <Setter TargetName="Inner"
                                        Property="Fill"
                                        Value="Gray" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>


    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Button x:Name="btnBack"
                Content="🔙 Orqaga"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,0,0,12"
                Padding="8,4"
                md:ButtonAssist.CornerRadius="4"
                Background="{DynamicResource OtherBackground}"
                Click="BtnBack_Click"/>

        <TabControl Margin="10" Grid.Row="1" x:Name="tabControl">
            <TabItem Header="Valyutalar" >

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <!-- Header -->
                        <RowDefinition Height="*" />
                        <!-- Scrollable list -->
                        <RowDefinition Height="Auto" />
                        <!-- Buttons -->
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Border Grid.Row="0"
                            BorderBrush="LightGray"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="10"
                            Margin="0 5 17 0"
                            Background="White"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="90" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Odatiy"
                                       FontWeight="SemiBold"
                                       Grid.Column="1"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="Faol"
                                       FontWeight="SemiBold"
                                       Grid.Column="2"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="O'zgaruvchan"
                                       FontWeight="SemiBold"
                                       Grid.Column="3"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="Nomi"
                                       FontWeight="SemiBold"
                                       Grid.Column="4"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="Kodi"
                                       Grid.Column="5"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="Belgisi"
                                       Grid.Column="6"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="Kurs"
                                       Grid.Column="7"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="Amallar"
                                       Grid.Column="8"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Grid>
                    </Border>

                    <!-- Scrollable valyuta ro‘yxati -->
                    <ScrollViewer Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  Margin="0,5">
                        <ItemsControl ItemsSource="{Binding Currencies}"
                                      AllowDrop="True"
                                      Drop="ItemsControl_Drop">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="LightGray"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Padding="10"
                                            Margin="0,5"
                                            Background="White"
                                            SnapsToDevicePixels="True">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="20" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="2*" />
                                                <ColumnDefinition Width="2*" />
                                                <ColumnDefinition Width="2*" />
                                                <ColumnDefinition Width="2*" />
                                                <ColumnDefinition Width="90" />
                                            </Grid.ColumnDefinitions>

                                            <fa:IconImage Icon="GripVertical"
                                                          Width="16"
                                                          Height="16"
                                                          Cursor="SizeAll"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Left"
                                                          Margin="4"
                                                          MouseDown="DragHandle_MouseDown"
                                                          MouseMove="DragHandle_MouseMove"
                                                          MouseUp="DragHandle_MouseUp"
                                                          ToolTip="Joyini o‘zgartirish" />

                                            <RadioButton GroupName="DefaultCurrency"
                                                         IsChecked="{Binding IsDefault}"
                                                         IsEnabled="{Binding IsActive}"
                                                         ToolTip="Default valyuta"
                                                         Grid.Column="1"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Center"
                                                         Style="{StaticResource RadioDotStyle}" />

                                            <CheckBox IsChecked="{Binding IsActive}"
                                                      Style="{Binding PersistentCheckBoxStyle}"
                                                      Grid.Column="2"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />

                                            <CheckBox IsChecked="{Binding IsEditable}"
                                                      Grid.Column="3"
                                                      Style="{Binding PersistentCheckBoxStyle}"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />

                                            <TextBox Text="{Binding Name}"
                                                     BorderThickness="0"
                                                     HorizontalAlignment="Center"
                                                     Grid.Column="4" />

                                            <TextBox Text="{Binding Code}"
                                                     BorderThickness="0"
                                                     HorizontalAlignment="Center"
                                                     Grid.Column="5" />

                                            <TextBox Text="{Binding Symbol}"
                                                     BorderThickness="0"
                                                     HorizontalAlignment="Center"
                                                     Grid.Column="6" />

                                            <TextBox Text="{Binding ExchangeRate}"
                                                     BorderThickness="0"
                                                     HorizontalAlignment="Center"
                                                     Grid.Column="7" />

                                            <Button Content="O‘chirish"
                                                    Grid.Column="8"
                                                    HorizontalAlignment="Center"
                                                    BorderThickness="0"
                                                    Command="{Binding DataContext.RemoveCurrencyCommand, ElementName=RootPage}"
                                                    CommandParameter="{Binding}"
                                                    Background="Tomato"
                                                    Foreground="White"
                                                    Width="80"
                                                    Padding="5 "
                                                    ToolTip="Valyutani o‘chirish" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- Qo‘shish va Saqlash tugmalari -->
                    <StackPanel Grid.Row="2"
                                Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Margin="0,10,0,0">
                        <Button Content="Valyuta qo‘shish"
                                Command="{Binding AddCurrencyCommand}"
                                Width="140"
                                Height="35"
                                Background="#007ACC"
                                Foreground="White"
                                FontWeight="SemiBold">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="5"
                                            BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="10,2" />
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Content="Saqlash"
                                Command="{Binding SaveCurrenciesCommand}"
                                Width="140"
                                Height="35"
                                Margin="15 0 0 0"
                                Background="ForestGreen"
                                Foreground="White"
                                FontWeight="SemiBold">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="5"
                                            BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="10 2" />
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </TabItem>
            
            <TabItem Header="O'lchov birliklari">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <!-- Header -->
                        <RowDefinition Height="*" />
                        <!-- Scrollable list -->
                        <RowDefinition Height="Auto" />
                        <!-- Buttons -->
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Border Grid.Row="0"
                            BorderBrush="LightGray"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="10"
                            Margin="0 5 17 0"
                            Background="White"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="5*" />
                                <ColumnDefinition Width="90" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Odatiy"
                                       FontWeight="SemiBold"
                                       Grid.Column="1"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="Faol"
                                       FontWeight="SemiBold"
                                       Grid.Column="2"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="Nomi"
                                       FontWeight="SemiBold"
                                       Grid.Column="3"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="Belgilanish"
                                       Grid.Column="4"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="Batafsil"
                                       Grid.Column="5"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="Amallar"
                                       Grid.Column="6"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Grid>
                    </Border>

                    <!-- Scrollable valyuta ro‘yxati -->
                    <ScrollViewer Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  Margin="0,5">
                        <ItemsControl ItemsSource="{Binding UnitMeasures}"
                                      AllowDrop="True"
                                      Drop="ItemsControl_Drop">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="LightGray"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Padding="10"
                                            Margin="0,5"
                                            Background="White"
                                            SnapsToDevicePixels="True">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="20" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="2*" />
                                                <ColumnDefinition Width="5*" />
                                                <ColumnDefinition Width="90" />
                                            </Grid.ColumnDefinitions>

                                            <fa:IconImage Icon="GripVertical"
                                                          Width="16"
                                                          Height="16"
                                                          Cursor="SizeAll"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Left"
                                                          Margin="4"
                                                          MouseDown="DragHandle_MouseDown"
                                                          MouseMove="DragHandle_MouseMove"
                                                          MouseUp="DragHandle_MouseUp"
                                                          ToolTip="Joyini o‘zgartirish" />

                                            <RadioButton GroupName="DefaultUnitMeasure"
                                                         IsChecked="{Binding IsDefault}"
                                                         IsEnabled="{Binding IsActive}"
                                                         ToolTip="Default o'lchov birligi"
                                                         Grid.Column="1"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Center"
                                                         Style="{StaticResource RadioDotStyle}" />

                                            <CheckBox IsChecked="{Binding IsActive}"
                                                      Style="{Binding PersistentCheckBoxStyle}"
                                                      Grid.Column="2"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />

                                            <TextBox Text="{Binding Name}"
                                                     BorderThickness="0"
                                                     HorizontalAlignment="Center"
                                                     Grid.Column="3" />

                                            <TextBox Text="{Binding Symbol}"
                                                     BorderThickness="0"
                                                     HorizontalAlignment="Center"
                                                     Grid.Column="4" />

                                            <TextBox Text="{Binding Description}"
                                                     BorderThickness="0"
                                                     HorizontalAlignment="Center"
                                                     Grid.Column="5" />

                                            <Button Content="O‘chirish"
                                                    Grid.Column="6"
                                                    HorizontalAlignment="Center"
                                                    BorderThickness="0"
                                                    Command="{Binding DataContext.RemoveUnitMeasureCommand, ElementName=RootPage}"
                                                    CommandParameter="{Binding}"
                                                    Background="Tomato"
                                                    Foreground="White"
                                                    Width="80"
                                                    Padding="5 "
                                                    ToolTip="Valyutani o‘chirish" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- Qo‘shish va Saqlash tugmalari -->
                    <StackPanel Grid.Row="2"
                                Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Margin="0,10,0,0">
                        <Button Content="Birlik qo‘shish"
                                Command="{Binding AddUnitMeasureCommand}"
                                Width="140"
                                Height="35"
                                Background="#007ACC"
                                Foreground="White"
                                FontWeight="SemiBold">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="5"
                                            BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="10,2" />
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Content="Saqlash"
                                Command="{Binding SaveUnitMeasuresCommand}"
                                Width="140"
                                Height="35"
                                Margin="15 0 0 0"
                                Background="ForestGreen"
                                Foreground="White"
                                FontWeight="SemiBold">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="5"
                                            BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="10 2" />
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
