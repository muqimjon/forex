<UserControl
    x:Class="Forex.Wpf.Pages.SemiProducts.UserControls.EditableGridControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Forex.Wpf.Resources.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Forex.Wpf.Pages.SemiProducts.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!--  Header  -->
            <RowDefinition Height="4" />
            <!--    -->
            <RowDefinition Height="*" />
            <!--  DataGrid  -->
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                FontSize="14"
                FontWeight="Bold"
                Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" />

            <Button
                Grid.Column="1"
                Width="30"
                Padding="4"
                Command="{Binding AddCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Content="➕" />
        </Grid>


        <!--  DataGrid  -->
        <DataGrid
            Grid.Row="2"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            HeadersVisibility="Column"
            ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
            RowHeaderWidth="0"
            SelectionMode="Single">

            <DataGrid.Columns>
                <!--  Nomi  -->
                <DataGridTemplateColumn Header="Nomi">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <!--  Har doim ko‘rinadigan label  -->
                                <TextBlock VerticalAlignment="Center" Text="{Binding Name}" />

                                <!--  Edit mode: faqat IsEditing true bo‘lsa  -->
                                <TextBox
                                    VerticalAlignment="Center"
                                    Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>


                <!--  Kodi  -->
                <DataGridTextColumn Binding="{Binding Code, Mode=TwoWay}" Header="Kodi" />
                <DataGridTextColumn Binding="{Binding Measure, Mode=TwoWay}" Header="O‘lchov" />
                <DataGridTextColumn Binding="{Binding Quantity, Mode=TwoWay}" Header="Miqdor" />
                <DataGridTextColumn Binding="{Binding CostPrice, Mode=TwoWay}" Header="Narxi" />

                <!--  Amal  -->
                <DataGridTemplateColumn Header="Amal">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <controls:ActionButton
                                CommandParameter="{Binding}"
                                DeleteCommand="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                EditCommand="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>