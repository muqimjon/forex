<Page x:Class="Forex.Wpf.Pages.Processes.ProcessPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Forex.Wpf.Pages.Processes"
      xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:controls="clr-namespace:Forex.Wpf.Resources.UserControls"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProcessPage">

    <Page.Resources>
        <!--  DataGrid cell style  -->
        <Style x:Key="TransparentCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="BorderBrush" Value="Transparent" />
        </Style>

        <Style x:Key="InlineTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="InlineComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Page.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <!--  Page Header  -->
        <DockPanel LastChildFill="False">
            <Label
                Content="Ishlab chiqarish jarayoni oynasi"
                FontSize="18"
                FontWeight="Bold"
                Foreground="{StaticResource SecondaryTextColor}" />

            <Button
                x:Name="btnBack"
                Padding="8,0"
                Margin=" 5 0 10 0"
                md:ButtonAssist.CornerRadius="4"
                md:ElevationAssist.Elevation="Dp24"
                Background="{DynamicResource BackButtonBackground}"
                BorderBrush="{StaticResource BackButtonBorder}"
                Click="BtnBack_Click"
                Content="🔙 Orqaga"
                DockPanel.Dock="Right"
                Foreground="{StaticResource BackButtonForeground}" />
        </DockPanel>

        <!--  Main Content  -->
        <Grid Grid.Row="1">
            <Border Background="AliceBlue" CornerRadius="4" Margin="10">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="50"
                        Direction="280"
                        Opacity="0.2"
                        ShadowDepth="15"
                        Color="Black" />
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Sarlavha -->
                        <RowDefinition Height="4"/>
                        <RowDefinition Height="*"/>
                        <!-- DataGrid pastgacha cho‘ziladi -->
                    </Grid.RowDefinitions>

                    <!-- Yuqori sarlavha va tugma -->
                    <Grid Grid.Row="0" Margin="10,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="40" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="10,2"
                            FontSize="20"
                            FontWeight="SemiBold"
                            Foreground="{StaticResource PrimaryTextColor}"
                            Text="Ishlab chiqarish jarayoniga olingan yarim tayyor mahsulotlar ro'yxati" />

                        <Button
                            Grid.Column="2"
                            Padding="4"
                            Background="Transparent"
                            BorderThickness="0"
                            FontSize="18"
                            ToolTip="Yangi mahsulot qo‘shish"
                            Command="{Binding AddProductCommand}"
                            Content="➕"
                            Foreground="{StaticResource PrimaryTextColor}" />
                    </Grid>

                    <!-- Asosiy DataGrid -->
                    <DataGrid
                        Grid.Row="2"
                        Margin="10"
                        AutoGenerateColumns="False"
                        Background="#f1f1f1"
                        BorderThickness="0"
                        CanUserAddRows="False"
                        EnableColumnVirtualization="True"
                        EnableRowVirtualization="False"
                        HeadersVisibility="Column"
                        IsReadOnly="False"
                        ItemsSource="{Binding FilteredProducts}"
                        RowHeaderWidth="0"
                        RowHeight="36"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        ScrollViewer.CanContentScroll="True"
                        SelectedItem="{Binding SelectedProduct, Mode=TwoWay}"
                        SnapsToDevicePixels="True"
                        UseLayoutRounding="True"
                        VirtualizingPanel.IsVirtualizing="True"
                        VirtualizingPanel.VirtualizationMode="Recycling">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Center" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Style>

                            <Style x:Key="RightAlignedText" TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Right" />
                                <Setter Property="Margin" Value="0,0,5,0" />
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>

                            <!-- 🖼️ Rasm -->
                            <DataGridTemplateColumn Width="100" Header="Rasm">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image
                                            Width="80"
                                            Height="80"
                                            Source="{Binding Product.Image, Converter={StaticResource ImageFallbackConverter}}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- 🔢 Kod -->
                            <DataGridTemplateColumn Width="0.8*" Header="Kod">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox
                                            Padding="20,0,0,0"
                                            BorderThickness="0"
                                            DisplayMemberPath="Code"
                                            IsTextSearchEnabled="True"
                                            ItemsSource="{Binding DataContext.AvailableProducts, RelativeSource={RelativeSource AncestorType=Page}}"
                                            SelectedItem="{Binding Product, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- 🧾 Nomi -->
                            <DataGridTemplateColumn Width="1.5*" Header="Nomi">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox
                                            Padding="0"
                                            BorderThickness="0"
                                            DisplayMemberPath="Name"
                                            IsTextSearchEnabled="True"
                                            ItemsSource="{Binding DataContext.AvailableProducts, RelativeSource={RelativeSource AncestorType=Page}}"
                                            SelectedItem="{Binding Product, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- 📏 O‘lcham -->
                            <DataGridTemplateColumn Width="*" Header="O'lcham">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox
                                            Padding="20,0,0,0"
                                            BorderThickness="0"
                                            DisplayMemberPath="Type"
                                            IsTextSearchEnabled="True"
                                            ItemsSource="{Binding AvailableProductTypes}"
                                            SelectedItem="{Binding ProductType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Jami soni -->
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding TotalCount, StringFormat={}{0:N0}}"
                                ElementStyle="{StaticResource RightAlignedText}"
                                Header="Jami soni"
                                IsReadOnly="True" />

                            <!-- ⚙️ Actions -->
                            <DataGridTemplateColumn Width="0.2*" Header="">
                                <DataGridTemplateColumn.CellStyle>
                                    <StaticResource ResourceKey="TransparentCellStyle" />
                                </DataGridTemplateColumn.CellStyle>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <controls:ActionButton
                                            Margin="0,0,10,0"
                                            HorizontalContentAlignment="Center"
                                            CommandParameter="{Binding}"
                                            DeleteCommand="{Binding DataContext.DeleteProductCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            EditCommand="{Binding DataContext.EditProductCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            IsEditing="{Binding IsEditing, Mode=TwoWay}"
                                            SaveCommand="{Binding DataContext.SaveProductCommand, RelativeSource={RelativeSource AncestorType=Page}}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
        <Button Grid.Row="2"
                Margin="5 0 10 0"
                Height="28"
                Width="120"
                md:ButtonAssist.CornerRadius="4"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Right"
                md:ElevationAssist.Elevation="Dp24"
                Background="{StaticResource SuccessButtonBackground}"
                BorderBrush="{StaticResource SuccessButtonBorder}"
                Command="{Binding SubmitCommand}"
                Content="Yuborish"
                Foreground="{StaticResource SuccessButtonForeground}" />
    </Grid>
</Page>
