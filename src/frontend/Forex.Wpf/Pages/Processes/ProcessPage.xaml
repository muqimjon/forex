<Page x:Class="Forex.Wpf.Pages.Processes.ProcessPage" 
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Forex.Wpf.Pages.Processes" 
      xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes" 
      xmlns:controls="clr-namespace:Forex.Wpf.Resources.UserControls" 
      mc:Ignorable="d" 
      Title="ProcessPage" 
      d:DesignHeight="450" 
      d:DesignWidth="800">

    <Page.Resources>
        <Style x:Key="TransparentCellStyle" TargetType="DataGridCell">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="BorderBrush" Value="Transparent" />
        </Style>

        <Style x:Key="RightAlignedText" TargetType="TextBlock">
            <Setter Property="TextAlignment" Value="Right" />
            <Setter Property="Margin" Value="0 0 5 0" />
        </Style>

        <Style x:Key="ProductNameComboBoxStyle" TargetType="ComboBox">
            <Setter Property="ItemsSource" Value="{Binding DataContext.AvailableProducts, RelativeSource={RelativeSource AncestorType=Page}}" />
            <Setter Property="DisplayMemberPath" Value="Name" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="Black" />
        </Style>

        <Style x:Key="ProductCodeComboBoxStyle" TargetType="ComboBox">
            <Setter Property="ItemsSource" Value="{Binding DataContext.AvailableProducts, RelativeSource={RelativeSource AncestorType=Page}}" />
            <Setter Property="DisplayMemberPath" Value="Code" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="Black" />
        </Style>

        <!-- 🎯 Miqdori uchun TextBox style -->
        <Style x:Key="QuantityTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </Page.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 🔙 Back Button -->
        <DockPanel Grid.Row="0" LastChildFill="False">
            <Label Content="Ishlab chiqarish jarayoni oynasi"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="{StaticResource SecondaryTextColor}" />
            <Button x:Name="btnBack"
                    Padding="8 0"
                    Margin="5 0 10 0"
                    md:ButtonAssist.CornerRadius="4"
                    md:ElevationAssist.Elevation="Dp24"
                    Background="{DynamicResource BackButtonBackground}"
                    BorderBrush="{StaticResource BackButtonBorder}"
                    Click="BtnBack_Click"
                    Content="🔙 Orqaga"
                    DockPanel.Dock="Right"
                    Foreground="{StaticResource BackButtonForeground}" />
        </DockPanel>

        <!-- 📋 DataGrid -->
        <DataGrid Grid.Row="1"
                  Margin="10"
                  AutoGenerateColumns="False"
                  CanUserAddRows="True"
                  BorderThickness="2"
                  HeadersVisibility="Column"
                  ItemsSource="{Binding EntryToProcessByProduct}"
                  RowHeight="50"
                  CellStyle="{StaticResource TransparentCellStyle}"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">

            <DataGrid.Columns>
                <!-- 🖼️ Surat -->
                <DataGridTemplateColumn Width="100" Header="Rasm">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Width="80"
                                   Height="80"
                                   Source="{Binding Product.Image, Converter={StaticResource ImageFallbackConverter}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- 🔢 Kod -->
                <DataGridComboBoxColumn Width="0.8*" Header="Kod"
                                        SelectedItemBinding="{Binding Product, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ElementStyle="{StaticResource ProductCodeComboBoxStyle}"
                                        EditingElementStyle="{StaticResource ProductCodeComboBoxStyle}" />

                <!-- 🧾 Nomi -->
                <DataGridComboBoxColumn Width="1.5*" Header="Nomi"
                                        SelectedItemBinding="{Binding Product, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ElementStyle="{StaticResource ProductNameComboBoxStyle}"
                                        EditingElementStyle="{StaticResource ProductNameComboBoxStyle}" />

                <!-- 📏 O‘lcham -->
                <DataGridTemplateColumn Width="*" Header="O'lcham">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding Product.ProductTypes}"
                                      SelectedItem="{Binding Product.SelectedType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      DisplayMemberPath="Type"
                                      Style="{StaticResource ProductNameComboBoxStyle}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- 🔢 Miqdori -->
                <DataGridTemplateColumn Width="*" Header="Miqdori">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource QuantityTextBoxStyle}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- ✅ Submit Button -->
        <Button Grid.Row="2"
                Margin="5 0 10 0"
                Height="28"
                Width="120"
                md:ButtonAssist.CornerRadius="4"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Right"
                md:ElevationAssist.Elevation="Dp24"
                Background="{StaticResource SuccessButtonBackground}"
                BorderBrush="{StaticResource SuccessButtonBorder}"
                Command="{Binding SubmitCommand}"
                Content="Yuborish"
                Foreground="{StaticResource SuccessButtonForeground}" />
    </Grid>
</Page>
