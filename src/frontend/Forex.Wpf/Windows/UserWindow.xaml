<Window
    x:Class="Forex.Wpf.Windows.UserWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:Forex.Wpf.Resources.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Forex.Wpf.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="Yangi foydalanuvchi qo'shish"
    Width="400"
    Height="430"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterScreen"
    WindowStyle="SingleBorderWindow"
    Background="{DynamicResource SurfaceCard}"
    mc:Ignorable="d">

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BooleanToVisibility" />
    </Window.Resources>

    <Grid Margin="10" TextElement.Foreground="{DynamicResource InkPrimary}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Rol  -->
        <TextBlock
            x:Name="lblRole"
            Grid.Row="0"
            Grid.Column="0"
            Margin="0,5"
            VerticalAlignment="Center"
            Text="Rol:"
            Visibility="Collapsed" />
        <Border
            x:Name="brRole"
            Grid.Row="0"
            Grid.Column="1"
            Margin="0,5"
            BorderBrush="{DynamicResource HairlineBorder}"
            BorderThickness="1"
            CornerRadius="4"
            Visibility="Collapsed">
            <ComboBox
                x:Name="cbWindowRole"
                Padding="4"
                VerticalContentAlignment="Center"
                md:TextFieldAssist.DecorationVisibility="Collapsed"
                Background="Transparent"
                BorderThickness="0"
                FontSize="14"
                IsEditable="False"
                Style="{StaticResource MaterialDesignComboBox}" />
        </Border>

        <!--  Nomi  -->
        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            Margin="0,5"
            VerticalAlignment="Center"
            Text="Nomi:" />
        <Border
            Grid.Row="1"
            Grid.Column="1"
            Margin="0,5"
            BorderBrush="{DynamicResource HairlineBorder}"
            BorderThickness="1"
            CornerRadius="4">
            <TextBox
                x:Name="txtName"
                Padding="4"
                BorderThickness="0"
                FontSize="14" />
        </Border>

        <!--  Telefon  -->
        <TextBlock
            Grid.Row="2"
            Grid.Column="0"
            Margin="0,5"
            VerticalAlignment="Center"
            Text="Telefon raqami:" />
        <Border
            Grid.Row="2"
            Grid.Column="1"
            Margin="0,5"
            BorderBrush="{DynamicResource HairlineBorder}"
            BorderThickness="1"
            CornerRadius="4">
            <TextBox
                x:Name="txtPhone"
                Padding="4"
                BorderThickness="0"
                FontSize="14"
                GotFocus="TxtPhone_GotFocus"
                TextChanged="TxtPhone_TextChanged" />
        </Border>

        <!--  Manzili  -->
        <TextBlock
            Grid.Row="3"
            Grid.Column="0"
            Margin="0,5"
            VerticalAlignment="Center"
            Text="Manzili:" />
        <Border
            Grid.Row="3"
            Grid.Column="1"
            Margin="0,5"
            BorderBrush="{DynamicResource HairlineBorder}"
            BorderThickness="1"
            CornerRadius="4">
            <TextBox
                x:Name="txtAddress"
                Padding="4"
                BorderThickness="0"
                FontSize="14" />
        </Border>

        <!--  Hisob valyutasi  -->
        <TextBlock
            Grid.Row="4"
            Grid.Column="0"
            Margin="0,5"
            VerticalAlignment="Center"
            Text="Hisob valyutasi:" />
        <Border
            Grid.Row="4"
            Grid.Column="1"
            Margin="0,5"
            BorderBrush="{DynamicResource HairlineBorder}"
            BorderThickness="1"
            CornerRadius="4">
            <ComboBox
                x:Name="cbCurrency"
                Padding="4"
                VerticalContentAlignment="Center"
                md:TextFieldAssist.DecorationVisibility="Collapsed"
                Background="Transparent"
                BorderThickness="0"
                DisplayMemberPath="Code"
                FontSize="14"
                Style="{StaticResource MaterialDesignComboBox}" />
        </Border>

        <!--  Boshlang‘ich balans  -->
        <TextBlock
            Grid.Row="5"
            Grid.Column="0"
            Margin="0,5"
            VerticalAlignment="Center"
            Text="Boshlang'ich balans:" />
        <Grid Grid.Row="5" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Border
                    Margin="0,5"
                    BorderBrush="{DynamicResource HairlineBorder}"
                    BorderThickness="1"
                    CornerRadius="4">
                    <Grid>
                        <TextBox
                            x:Name="txtBeginningSum"
                            Padding="4"
                            BorderThickness="0"
                            FontSize="14"
                            Foreground="{DynamicResource Danger}"
                            Tag="Qarzdorlik" />
                        <TextBlock
                            Margin="8,0,0,0"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource Danger}"
                            IsHitTestVisible="False"
                            Text="{Binding ElementName=txtBeginningSum, Path=Tag}"
                            Visibility="{Binding Text.IsEmpty, ElementName=txtBeginningSum, Converter={StaticResource BooleanToVisibility}}" />
                    </Grid>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <Border
                    Margin="0,5"
                    BorderBrush="{DynamicResource HairlineBorder}"
                    BorderThickness="1"
                    CornerRadius="4">
                    <Grid>
                        <TextBox
                            x:Name="txtBeginningSum2"
                            Padding="4"
                            BorderThickness="0"
                            FontSize="14"
                            Tag="Haqdorlik" />
                        <TextBlock
                            Margin="8,0,0,0"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource InkFaint}"
                            IsHitTestVisible="False"
                            Text="{Binding ElementName=txtBeginningSum2, Path=Tag}"
                            Visibility="{Binding Text.IsEmpty, ElementName=txtBeginningSum2, Converter={StaticResource BooleanToVisibility}}" />
                    </Grid>
                </Border>
            </StackPanel>

        </Grid>

        <!--  Qo‘shimcha ma‘lumot  -->
        <TextBlock
            Grid.Row="6"
            Grid.Column="0"
            Margin="0,5"
            VerticalAlignment="Center"
            Text="Qo‘shimcha ma‘lumot:" />
        <Border
            Grid.Row="6"
            Grid.Column="1"
            Margin="0,5"
            BorderBrush="{DynamicResource HairlineBorder}"
            BorderThickness="1"
            CornerRadius="4">
            <TextBox
                x:Name="txtDescription"
                Padding="4"
                BorderThickness="0"
                FontSize="14" />
        </Border>

        <!--  Tugmalar  -->
        <StackPanel
            Grid.Row="7"
            Grid.ColumnSpan="2"
            Margin="0,5,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                x:Name="btnSave"
                Width="110"
                Height="30"
                Margin="5"
                Click="Button_Click"
                Content="Saqlash">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="{DynamicResource Accent}" />
                        <Setter Property="Foreground" Value="{DynamicResource OnAccent}" />
                        <Setter Property="BorderBrush" Value="{DynamicResource Accent}" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Padding" Value="6,3" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Cursor" Value="Hand" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Accent}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button
                Width="110"
                Margin="5"
                Content="Bekor qilish"
                IsCancel="True">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="{DynamicResource SurfaceChip}" />
                        <Setter Property="Foreground" Value="{DynamicResource InkSecondary}" />
                        <Setter Property="BorderBrush" Value="{DynamicResource HairlineBorder}" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Padding" Value="6,3" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Cursor" Value="Hand" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource HairlineBorder}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

    </Grid>
</Window>
